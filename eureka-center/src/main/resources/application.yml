spring:
  application:
    name: eureka-center
  freemarker:
    prefer-file-system-access: false
server:
  port: 1001

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false